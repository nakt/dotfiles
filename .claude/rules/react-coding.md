---
description: React のコード作業時に参照するガイドラインへのポインタ
paths:
  - "**/*.tsx"
  - "**/*.jsx"
---

React のコード作業時は `/react-dev-guide` スキルを呼び出してガイドラインを参照すること。

`.tsx` では typescript-dev-guide と react-dev-guide の両方が適用され、矛盾する場合は react-dev-guide を優先する。
